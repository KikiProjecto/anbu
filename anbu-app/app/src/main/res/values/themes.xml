<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window background is dark so the pre-Compose frame does not flash white
         before AnbuTheme's dark surface draws. -->
    <style name="Theme.Anbu" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/anbu_window_background</item>
    </style>
</resources>
