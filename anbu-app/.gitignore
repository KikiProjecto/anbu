# One ignore file, at the repository root. Keeping a second copy here meant two
# lists that could drift apart — and drift in an ignore file is how a 25GB GGUF
# or a keystore ends up in a commit. See ../.gitignore.
